{
  "name": "vite-micro-frontends",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:artist-list": "pnpm --filter artist-list dev",
    "dev:artist-details": "pnpm --filter artist-details dev",
    "dev:ui": "pnpm --filter ui dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r --parallel preview",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 